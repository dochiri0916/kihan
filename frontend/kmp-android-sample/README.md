# KMP Android Sample

간단한 Kotlin Compose Multiplatform 안드로이드 앱 예시입니다.

## 실행

1. Android Studio에서 `frontend/kmp-android-sample` 폴더를 프로젝트로 엽니다.
2. `composeApp` 모듈을 Android 디바이스/에뮬레이터로 실행합니다.

## 포함 내용

- 공용 UI (`commonMain`) 기반 화면 구성
- Android 엔트리포인트 (`MainActivity`)
- 간단한 일정 리스트 UI + 완료 토글
